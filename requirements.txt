# Déploiement du cerveau (brain/) depuis la racine - Railway installe ceci.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
python-multipart>=0.0.7

# Couche IA optionnelle par API (BRAIN_MODE=claude). Le cerveau local passe
# par Ollama (aucune lib Python requise, il tourne à part).
anthropic>=0.40
