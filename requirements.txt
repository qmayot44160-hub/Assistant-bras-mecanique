# Déploiement du cerveau (brain/) depuis la racine - Railway installe ceci.
# Roues CPU pré-compilées pour llama.cpp (évite une compilation à la build) :
--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
python-multipart>=0.0.7

# Cerveau local (modèle qui pense sur le serveur)
llama-cpp-python>=0.3.2
huggingface_hub>=0.24

# Couche IA optionnelle par API (BRAIN_MODE=claude)
anthropic>=0.40
